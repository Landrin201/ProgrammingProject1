
/**
  Project Name: <br>
  Write a description of class TemplateOfClassWithMainMethod here.  E.g.,
  The TemplateOfClassWithMainMethod provides the main function for the
  ... program, which does ....
  <br> <br>
  Created: <br>
    (date),  Marc Kuniansky 
    <br> <br>
  Modifications: <br>
    (date), Marc Kuniansky, Modified to .... <br>
  
  @author Marc Kuniansky (with assistance from)
  @version (a version number or a date)
 **/
public class TemplateOfClassWithMainMethod
{ //Begin class
    /**
       This is the main function.  It initiates the execution of the
       ... program.
       @param    String[] args is probably not used in this program
     **/
    public static void main(String[] args)
    { //Begin main
        // Construct one or more objects, then get the ball rolling by
        // invoking one or more methods on those objects.
        NormalClassTemplate anObject = new NormalClassTemplate();

        // The sample method in NormalClassTemplate expects an integer
        // parameter, so pass it an arbitrary integer for now.
        anObject.invokeAMethod(42);

    }//End main

}//End class
